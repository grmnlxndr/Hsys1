{% extends 'HSYSMainBundle:Sangre:sangrebase.html.twig' %}

{% block title %}Modificar Estado{% endblock %}

{% block contenido %}
 
{% include 'HSYSMainBundle:Sangre:datosdeunidadver.html.twig' with {'unidades': unidades} %}
 
 <form id="buscar_form" name="buscar_form" method="post" action="{{ path('modificarestado_unidad') }}">
    <table>
        <tr>
            <td><label for="criterio">Criterio: </label></td>
            <td><select name="buscar" id="buscar" style="width: 160px">
                    {% for estado in estados %}
                    <option value="{{estado}}">{{estado}}</option>
                    {% endfor %}
                </select></td>
            <td><label>Búsqueda: </label></td>  
            <td><input type="submit" value="Buscar"/></td>
        </tr>
    </table>
</form>
<br/>


{%endblock%}
