{% extends 'HSYSMainBundle:Sangre:sangrebase.html.twig' %}

{% block title %}Buscar Por Estado{% endblock %}

{% block contenido %}
 <h3>Buscar unidad Por Estado:</h3>
    <form id="buscar_form" name="buscar_form" method="post" action="{{ path('buscar_unidad_estado') }}">
    <table>
        <tr>
            <td><label for="criterio">Criterio: </label></td>
            <td><select name="buscar" id="buscar" style="width: 160px">
                    {% for estado in estados %}
                    <option value="{{estado}}">{{estado}}</option>
                    {% endfor %}
                </select></td>
            <td><label>Búsqueda: </label></td>  
            <td><input type="submit" value="Buscar"/></td>
        </tr>
    </table>
</form>
<br/>
{% if metodo %}
{% include 'HSYSMainBundle:Sangre:resultados.html.twig' with {'unidades': unidades} %}
{% endif %}



{% endblock %}